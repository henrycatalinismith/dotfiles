[alias]
	a    = add
	ap   = add --patch
	b    = branch --verbose
	ba   = branch --all
	bc   = branch --contains
	br   = branch --remote
	c    = "!f() { git commit -am \"$*\"; }; f"
	ci   = "!f() { git commit -m \"$*\"; }; f"
	cia  = commit --amend
	co   = checkout
	cow  = "!f() { cowsay \"$*\" | git commit -a -F -; }; f"
	c-   = checkout --
	cp   = cherry-pick
	d    = diff
	dc   = diff --cached
	g    = grep -n
	gi   = grep -ni
	git  = !git $1
	ig   = ls-files --others -i --exclude-standard
	l    = log --graph --stat --decorate
	lp   = log --patch
	lod  = log --oneline --decorate
	lodg = log --oneline --decorate --graph
	m    = merge
	re   = "!f() { git rh $1; git c- $1; }; f"
	rh   = reset HEAD
	s    = status --short --branch
	sad  = submodule add
	up   = "!git remote update -p; git merge --ff-only @{u}"

[clean]
	requireForce = false

[color]
	branch   = true
	diff     = true
	decorate = true
	grep     = true
	pager    = true
	status   = true

[color "branch"]
	current = magenta reverse
	local = magenta
	remote = yellow

[color "decorate"]
	commit       = cyan
	branch       = magenta
	remotebranch = magenta bold
	tag          = red
	stash        = yellow
	HEAD         = cyan

[color "diff"]
	frag       = magenta
	meta       = white
	new        = green bold
	old        = red bold
	whitespace = white reverse

[color "grep"]
	filename   = blue
	linenumber = green
	match      = magenta bold
	separator  = cyan

[color "status"]
	branch    = magenta
	header    = blue
	added     = magenta
	changed   = magenta
	untracked = cyan

[core]
	autocrlf = false
	editor = vim
	pager = delta
	whitespace = trailing-space,space-before-tab
	excludesfile = ~/.config/git/ignore

[delta]
	commit-decoration-style = bold ul
	file-style = bold brightCyan
	hunk-header-decoration-style = none
	features = line-numbers decorations
	plus-style = syntax "#003336"
	minus-style = syntax "#3d1a37"
	line-numbers = true
	line-numbers-minus-style = "#F0FFF0"
	line-numbers-zero-style = "#F0FFF0"
	line-numbers-plus-style = "#F0FFF0"
	line-numbers-left-format = "{nm:>4}┊"
	line-numbers-right-format = "{np:>4}│"
	line-numbers-left-style = blue
	line-numbers-right-style = blue
	syntax-theme = Solarized (dark)


[delta "decorations"]
	commit-decoration-style = bold yellow box ul
	file-style = bold yellow ul
	file-decoration-style = none
	hunk-header-decoration-style = cyan box ul

[delta "line-numbers"]
	line-numbers-left-style = cyan
	line-numbers-right-style = cyan
	line-numbers-minus-style = 124
	line-numbers-plus-style = 28

[includeIf "gitdir:~/soundtrap/"]
    path = ~/soundtrap/.gitconfig

[init]
	defaultBranch = trunk

[log]
	date = iso

[user]
	name = Henry Catalini Smith
	email = henry@catalinismith.com

